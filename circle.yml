machine:
  node:
    version: 0.10.32
  environment:
    NODE_ENV: development
checkout:
  post:
    - git submocule sync
dependencies:
  override:
    - npm install
test:
  post:
    - npm run mocha:
        environment:
          NODE_ENV: development